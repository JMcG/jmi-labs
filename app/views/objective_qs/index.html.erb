<%- model_class = ObjectiveQ -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:organism_species) %></th>
      <th><%= model_class.human_attribute_name(:specimen_typea) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @objective_qs.each do |objective_q| %>
      <tr>
        <td><%= link_to objective_q.id, objective_q_path(objective_q) %></td>
        <td><%= objective_q.organism_species %></td>
        <td><%= objective_q.specimen_typea %></td>
        <td><%=l objective_q.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_objective_q_path(objective_q), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      objective_q_path(objective_q),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_objective_q_path,
            :class => 'btn btn-primary' %>
